---
version: "3"
services:
  identityprovider:
    build:
      context: .
      dockerfile: Dockerfile
      image: identityprovider
    ports:
      - 8080:80
    volumes:
      - ./resources/config/config.php:/var/simplesamlphp/config/config.php
      - ./resources/config/authsources.php:/var/simplsesamlphp/config/authsources.php
